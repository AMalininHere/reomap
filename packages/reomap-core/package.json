{
  "name": "@reomap/core",
  "version": "0.0.0",
  "scripts": {
    "build": "yarn build:babel && yarn build:types && yarn build:copy",
    "build:babel": "babel ./src --config-file ../../babel.config.js --out-dir ./build --extensions .ts,.tsx",
    "build:types": "tsc -p tsconfig.build.json",
    "build:copy": "node ../../scripts/copy-files.js"
  },
  "peerDependencies": {
    "react": "^16.8.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
